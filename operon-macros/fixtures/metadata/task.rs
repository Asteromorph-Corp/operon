pub const fn task_beta_meta() -> operon::__private::TaskMetadata<1usize> {
    operon::__private::TaskMetadata {
        id: "beta",
        dims: ["i"],
        spawn_dim: Some("j"),
        priority: &[],
    }
}

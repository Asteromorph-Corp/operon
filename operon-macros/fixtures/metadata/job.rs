pub const fn job_beta_meta() -> operon::__private::JobMetadata<1usize> {
    operon::__private::JobMetadata {
        id: "beta",
        dims: ["i"],
        spawn_dim: Some("j"),
        priority: &[],
    }
}

name: Rust

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Install nightly rustfmt
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt

      - name: Configure Cargo auth
        env:
          KELLNR_REGISTRY_URL: ${{ secrets.KELLNR_REGISTRY_URL }}
        run: |
          mkdir -p ~/.cargo
          cat > ~/.cargo/config.toml <<EOF
          [registries.kellnr]
          index = "sparse+https://${KELLNR_REGISTRY_URL}/api/v1/crates/"
          credential-provider = ["cargo:token"]
          EOF

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo +nightly fmt --all -- --check

      - name: Run cargo clippy
        env:
          CARGO_REGISTRIES_KELLNR_TOKEN: ${{ secrets.KELLNR_READONLY_TOKEN }}
        run: cargo +stable clippy --workspace --locked --all-features --all-targets -- -D warnings

      - name: Check documentation
        env:
          RUSTDOCFLAGS: "-D warnings"
          CARGO_REGISTRIES_KELLNR_TOKEN: ${{ secrets.KELLNR_READONLY_TOKEN }}
        run: cargo +stable doc --workspace --locked --no-deps --document-private-items

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Configure Cargo auth
        env:
          KELLNR_REGISTRY_URL: ${{ secrets.KELLNR_REGISTRY_URL }}
        run: |
          mkdir -p ~/.cargo
          cat > ~/.cargo/config.toml <<EOF
          [registries.kellnr]
          index = "sparse+https://${KELLNR_REGISTRY_URL}/api/v1/crates/"
          credential-provider = ["cargo:token"]
          EOF

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Run tests
        env:
          CARGO_REGISTRIES_KELLNR_TOKEN: ${{ secrets.KELLNR_READONLY_TOKEN }}
        run: cargo test --workspace --locked --all-features --all-targets

mod resolution_definition;
mod resolution_enum;

mod impl_enum_from_resolution;
mod impl_resolution;
mod impl_resolution_enum;
mod impl_resolution_sql;

mod mod_resolution;
pub(super) use mod_resolution::*;
